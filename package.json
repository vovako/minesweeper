{
  "name": "minesweeper",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "scss": "sass --watch scss:public/css",
    "sync": "browser-sync start --proxy localhost:3000 --files public",
    "nodemon": "nodemon server.js",

    "dev": "start npm run nodemon && start npm run scss && start npm run sync",
    "start": "node server.js"
  },
  "author": "Vladimir Kondaurov",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "nodemon": "^2.0.20",
    "sass": "^1.56.2"
  }
}
